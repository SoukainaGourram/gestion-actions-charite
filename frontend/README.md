# Frontend folder

This folder contains the Thymeleaf view templates for the application.

- `templates/`: all HTML pages used by the Spring Boot backend.

The backend builds these templates into the classpath using the Maven resource configuration in `../backend/pom.xml`.
